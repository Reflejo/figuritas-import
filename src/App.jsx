export default function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">GitHub Pages + React</p>
        <h1>Figuritas Import</h1>
        <p className="lead">
          A lightweight React starter configured to build and deploy cleanly on GitHub Pages.
        </p>
        <div className="card">
          <h2>Ready to customize</h2>
          <p>Replace this landing page with your app and keep the same deployment flow.</p>
        </div>
      </section>
    </main>
  );
}
